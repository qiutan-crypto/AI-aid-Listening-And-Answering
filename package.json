{
  "name": "ai-aid-listening-and-answering",
  "version": "0.1.0",
  "description": "英语对话辅助：实时英文字幕 + AI 回答提示",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && for f in src/*.js src/ai/*.js public/*.js; do node --check $f || exit 1; done"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@google/genai": "^2.24.0",
    "express": "^4.21.2",
    "mammoth": "^1.12.3",
    "unpdf": "^1.8.1",
    "ws": "^8.18.0"
  }
}
